name: MS4 Azure CD

on:
  push:
    branches:
      - main
    paths:
      - "ms4/**"
      - ".github/workflows/ms4-azure-cd.yml"
  pull_request:
    branches:
      - main
    paths:
      - "ms4/**"
      - ".github/workflows/ms4-azure-cd.yml"
  workflow_dispatch:

permissions:
  contents: read

env:
  IMAGE_NAME: ${{ vars.MS4_IMAGE_NAME || 'ms4-n8n' }}

jobs:
  validate:
    name: Validate MS4
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Validate N8N workflow exports
        run: |
          for workflow in ms4/n8n-workflows/*.json; do
            jq empty "$workflow"
          done

      - name: Build MS4 image
        uses: docker/build-push-action@v6
        with:
          context: ./ms4
          push: false
          tags: ms4-n8n:test

  build-and-deploy:
    name: Build image and deploy
    runs-on: ubuntu-latest
    needs: validate
    if: github.event_name != 'pull_request'
    environment: production
    concurrency:
      group: ms4-production
      cancel-in-progress: false

    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Validate deployment configuration
        env:
          MS4_ACR_LOGIN_SERVER: ${{ secrets.MS4_ACR_LOGIN_SERVER }}
          MS4_ACR_USERNAME: ${{ secrets.MS4_ACR_USERNAME }}
          MS4_ACR_PASSWORD: ${{ secrets.MS4_ACR_PASSWORD }}
          MS4_VM_HOST: ${{ secrets.MS4_VM_HOST }}
          MS4_VM_USER: ${{ secrets.MS4_VM_USER }}
          MS4_VM_SSH_KEY: ${{ secrets.MS4_VM_SSH_KEY }}
          MS4_N8N_HOST: ${{ vars.MS4_N8N_HOST }}
          MS4_N8N_EDITOR_BASE_URL: ${{ vars.MS4_N8N_EDITOR_BASE_URL }}
          MS4_WEBHOOK_URL: ${{ vars.MS4_WEBHOOK_URL }}
          MS4_N8N_ENCRYPTION_KEY: ${{ secrets.MS4_N8N_ENCRYPTION_KEY }}
          MS4_N8N_BASIC_AUTH_USER: ${{ vars.MS4_N8N_BASIC_AUTH_USER }}
          MS4_N8N_BASIC_AUTH_PASSWORD: ${{ secrets.MS4_N8N_BASIC_AUTH_PASSWORD }}
          MS4_MS1_GRAPHQL_URL: ${{ vars.MS4_MS1_GRAPHQL_URL }}
          MS4_MS2_BASE_URL: ${{ vars.MS4_MS2_BASE_URL }}
          MS4_MS3_BASE_URL: ${{ vars.MS4_MS3_BASE_URL }}
          MS4_FCM_API_URL: ${{ vars.MS4_FCM_API_URL }}
        run: |
          test -n "$MS4_ACR_LOGIN_SERVER" || { echo "::error::MS4_ACR_LOGIN_SERVER secret is required"; exit 1; }
          test -n "$MS4_ACR_USERNAME" || { echo "::error::MS4_ACR_USERNAME secret is required"; exit 1; }
          test -n "$MS4_ACR_PASSWORD" || { echo "::error::MS4_ACR_PASSWORD secret is required"; exit 1; }
          test -n "$MS4_VM_HOST" || { echo "::error::MS4_VM_HOST secret is required"; exit 1; }
          test -n "$MS4_VM_USER" || { echo "::error::MS4_VM_USER secret is required"; exit 1; }
          test -n "$MS4_VM_SSH_KEY" || { echo "::error::MS4_VM_SSH_KEY secret is required"; exit 1; }
          test -n "$MS4_N8N_HOST" || { echo "::error::MS4_N8N_HOST variable is required"; exit 1; }
          test -n "$MS4_N8N_EDITOR_BASE_URL" || { echo "::error::MS4_N8N_EDITOR_BASE_URL variable is required"; exit 1; }
          test -n "$MS4_WEBHOOK_URL" || { echo "::error::MS4_WEBHOOK_URL variable is required"; exit 1; }
          test -n "$MS4_N8N_ENCRYPTION_KEY" || { echo "::error::MS4_N8N_ENCRYPTION_KEY secret is required"; exit 1; }
          test -n "$MS4_N8N_BASIC_AUTH_USER" || { echo "::error::MS4_N8N_BASIC_AUTH_USER variable is required"; exit 1; }
          test -n "$MS4_N8N_BASIC_AUTH_PASSWORD" || { echo "::error::MS4_N8N_BASIC_AUTH_PASSWORD secret is required"; exit 1; }
          test -n "$MS4_MS1_GRAPHQL_URL" || { echo "::error::MS4_MS1_GRAPHQL_URL variable is required"; exit 1; }
          test -n "$MS4_MS2_BASE_URL" || { echo "::error::MS4_MS2_BASE_URL variable is required"; exit 1; }
          test -n "$MS4_MS3_BASE_URL" || { echo "::error::MS4_MS3_BASE_URL variable is required"; exit 1; }

      - name: Log in to Azure Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ secrets.MS4_ACR_LOGIN_SERVER }}
          username: ${{ secrets.MS4_ACR_USERNAME }}
          password: ${{ secrets.MS4_ACR_PASSWORD }}

      - name: Build and push MS4 image
        uses: docker/build-push-action@v6
        with:
          context: ./ms4
          push: true
          tags: |
            ${{ secrets.MS4_ACR_LOGIN_SERVER }}/${{ env.IMAGE_NAME }}:${{ github.sha }}
            ${{ secrets.MS4_ACR_LOGIN_SERVER }}/${{ env.IMAGE_NAME }}:latest

      - name: Deploy container to Azure VM
        env:
          MS4_ACR_LOGIN_SERVER: ${{ secrets.MS4_ACR_LOGIN_SERVER }}
          MS4_ACR_USERNAME: ${{ secrets.MS4_ACR_USERNAME }}
          MS4_ACR_PASSWORD: ${{ secrets.MS4_ACR_PASSWORD }}
          MS4_VM_HOST: ${{ secrets.MS4_VM_HOST }}
          MS4_VM_USER: ${{ secrets.MS4_VM_USER }}
          MS4_VM_SSH_KEY: ${{ secrets.MS4_VM_SSH_KEY }}
          IMAGE_TAG: ${{ github.sha }}
          MS4_N8N_HOST: ${{ vars.MS4_N8N_HOST }}
          MS4_N8N_PORT: ${{ vars.MS4_N8N_PORT || '5678' }}
          MS4_N8N_PROTOCOL: ${{ vars.MS4_N8N_PROTOCOL || 'https' }}
          MS4_N8N_EDITOR_BASE_URL: ${{ vars.MS4_N8N_EDITOR_BASE_URL }}
          MS4_WEBHOOK_URL: ${{ vars.MS4_WEBHOOK_URL }}
          MS4_N8N_ENCRYPTION_KEY: ${{ secrets.MS4_N8N_ENCRYPTION_KEY }}
          MS4_N8N_SECURE_COOKIE: ${{ vars.MS4_N8N_SECURE_COOKIE || (vars.MS4_N8N_PROTOCOL == 'http' && 'false' || 'true') }}
          MS4_N8N_BASIC_AUTH_ACTIVE: ${{ vars.MS4_N8N_BASIC_AUTH_ACTIVE || 'true' }}
          MS4_N8N_BASIC_AUTH_USER: ${{ vars.MS4_N8N_BASIC_AUTH_USER }}
          MS4_N8N_BASIC_AUTH_PASSWORD: ${{ secrets.MS4_N8N_BASIC_AUTH_PASSWORD }}
          MS4_N8N_IMPORT_WORKFLOWS: ${{ vars.MS4_N8N_IMPORT_WORKFLOWS || 'true' }}
          MS4_N8N_DIAGNOSTICS_ENABLED: ${{ vars.MS4_N8N_DIAGNOSTICS_ENABLED || 'false' }}
          MS4_N8N_VERSION_NOTIFICATIONS_ENABLED: ${{ vars.MS4_N8N_VERSION_NOTIFICATIONS_ENABLED || 'false' }}
          MS4_N8N_TEMPLATES_ENABLED: ${{ vars.MS4_N8N_TEMPLATES_ENABLED || 'false' }}
          MS4_N8N_PERSONALIZATION_ENABLED: ${{ vars.MS4_N8N_PERSONALIZATION_ENABLED || 'false' }}
          MS4_MS1_GRAPHQL_URL: ${{ vars.MS4_MS1_GRAPHQL_URL }}
          MS4_MS2_BASE_URL: ${{ vars.MS4_MS2_BASE_URL }}
          MS4_MS3_BASE_URL: ${{ vars.MS4_MS3_BASE_URL }}
          MS4_FCM_API_URL: ${{ vars.MS4_FCM_API_URL }}
        run: |
          install -m 700 -d ~/.ssh
          printf '%s\n' "$MS4_VM_SSH_KEY" > ~/.ssh/ms4_vm_key
          chmod 600 ~/.ssh/ms4_vm_key
          ssh-keyscan -H "$MS4_VM_HOST" >> ~/.ssh/known_hosts

          {
            printf 'MS4_IMAGE=%s/%s:%s\n' "$MS4_ACR_LOGIN_SERVER" "$IMAGE_NAME" "$IMAGE_TAG"
            printf 'MS4_N8N_HOST=%s\n' "$MS4_N8N_HOST"
            printf 'MS4_N8N_PORT=%s\n' "$MS4_N8N_PORT"
            printf 'MS4_N8N_PROTOCOL=%s\n' "$MS4_N8N_PROTOCOL"
            printf 'MS4_N8N_EDITOR_BASE_URL=%s\n' "$MS4_N8N_EDITOR_BASE_URL"
            printf 'MS4_WEBHOOK_URL=%s\n' "$MS4_WEBHOOK_URL"
            printf 'MS4_N8N_ENCRYPTION_KEY=%s\n' "$MS4_N8N_ENCRYPTION_KEY"
            printf 'MS4_N8N_SECURE_COOKIE=%s\n' "$MS4_N8N_SECURE_COOKIE"
            printf 'MS4_N8N_BASIC_AUTH_ACTIVE=%s\n' "$MS4_N8N_BASIC_AUTH_ACTIVE"
            printf 'MS4_N8N_BASIC_AUTH_USER=%s\n' "$MS4_N8N_BASIC_AUTH_USER"
            printf 'MS4_N8N_BASIC_AUTH_PASSWORD=%s\n' "$MS4_N8N_BASIC_AUTH_PASSWORD"
            printf 'MS4_N8N_IMPORT_WORKFLOWS=%s\n' "$MS4_N8N_IMPORT_WORKFLOWS"
            printf 'MS4_N8N_DIAGNOSTICS_ENABLED=%s\n' "$MS4_N8N_DIAGNOSTICS_ENABLED"
            printf 'MS4_N8N_VERSION_NOTIFICATIONS_ENABLED=%s\n' "$MS4_N8N_VERSION_NOTIFICATIONS_ENABLED"
            printf 'MS4_N8N_TEMPLATES_ENABLED=%s\n' "$MS4_N8N_TEMPLATES_ENABLED"
            printf 'MS4_N8N_PERSONALIZATION_ENABLED=%s\n' "$MS4_N8N_PERSONALIZATION_ENABLED"
            printf 'MS4_MS1_GRAPHQL_URL=%s\n' "$MS4_MS1_GRAPHQL_URL"
            printf 'MS4_MS2_BASE_URL=%s\n' "$MS4_MS2_BASE_URL"
            printf 'MS4_MS3_BASE_URL=%s\n' "$MS4_MS3_BASE_URL"
            printf 'MS4_FCM_API_URL=%s\n' "$MS4_FCM_API_URL"
          } > .env.deploy

          ssh -i ~/.ssh/ms4_vm_key "$MS4_VM_USER@$MS4_VM_HOST" \
            'sudo install -d -m 775 -o "$USER" -g "$(id -gn)" /opt/ms4'
          scp -i ~/.ssh/ms4_vm_key ms4/docker-compose.prod.yml "$MS4_VM_USER@$MS4_VM_HOST:/opt/ms4/docker-compose.yml"
          scp -i ~/.ssh/ms4_vm_key .env.deploy "$MS4_VM_USER@$MS4_VM_HOST:/opt/ms4/.env"

          printf '%s\n' "$MS4_ACR_PASSWORD" | ssh -i ~/.ssh/ms4_vm_key "$MS4_VM_USER@$MS4_VM_HOST" \
            "docker login '$MS4_ACR_LOGIN_SERVER' --username '$MS4_ACR_USERNAME' --password-stdin"

          ssh -i ~/.ssh/ms4_vm_key "$MS4_VM_USER@$MS4_VM_HOST" \
            "cd /opt/ms4 \
            && docker compose --env-file .env -f docker-compose.yml pull \
            && docker compose --env-file .env -f docker-compose.yml up -d --remove-orphans"
